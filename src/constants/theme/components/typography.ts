import { type ComponentToken } from 'antd/es/typography/style'

import { type AntdConfig } from '@/types'

export const Typography: AntdConfig<ComponentToken> = {
  titleMarginBottom: 0,
  titleMarginTop: 0,
}
